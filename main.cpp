#include <QApplication>

  #include "analogclock.h"

  int main(int argc, char *argv[])
  {
      QApplication app(argc, argv);
      SceneView clock;
      clock.show();
      clock.show();
      return app.exec();
  }
